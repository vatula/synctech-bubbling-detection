# Strategic Operational Directives for Pipeline Optimization

## SYSTEM PROTOCOL AND MANDATE ENFORCEMENT

You are currently operating under the stringent System Engineering Directives established in the AGENTS.md document. You are expressly forbidden from executing parallel tasks; your execution flow must remain completely atomic. Conversational filler and unsolicited qualitative reasoning are strictly banned. You must ensure that the structlog library handles all telemetry operations, and standard python `print()` operations must be eradicated from the source tree.

Every single task enumerated below concludes with a mandatory Static Analysis Gate and a Validation Gate. You must successfully execute the static analysis checks, output the exact phrase "Static Analysis: PASSED", and immediately halt generation to await authorization from the human operator.

---

## TASK 1: Dataset Robustness and Structural Encapsulation

**State Acknowledgment**: You must begin your response by explicitly stating: "Executing Task 1: Dataset Robustness and Structural Encapsulation."

**Execution Directive**:
1. Access and refactor `src/data/loader.py`. Locate the `__getitem__` method within the `BubblingDataset` class.
2. Delete the permissive and highly fragile fallback logic that triggers when the Albumentations transform fails to output a tensor structure (the logic that manually transposes the image and applies the `1/255.0` numerical scaling).
3. Replace this fallback logic with a strict validation block. If the output of the transform pipeline is not an instance of `torch.Tensor`, the script must instantly raise a rigorously typed `RuntimeError`, accompanied by a structured error payload detailing the failure mode.
4. Inject the `structlog` library into the module. Instantiate the module-level telemetry pipeline via `logger = structlog.get_logger()`.
5. Implement a logging payload within the class initialization that outputs the exact numerical count of the localized nominal images and bubbling images.
6. Access the empty package initialization files `src/data/__init__.py` and `src/models/__init__.py`. Populate these files with explicit `__all__` definitions, strictly exporting the primary classes (e.g., `BubblingDataset`, `BubblingClassifier`, `FastViTStudent`) to enforce rigid modular boundaries.

**Static Analysis Gate**:

Execute the command `ruff check --fix`. followed by `pyright`. You must resolve any syntax or typing errors before proceeding. Output: "Static Analysis: PASSED".

**Validation Gate:**

Halt generation completely. Ask the human operator for authorization to transition to Task 2.

---

## TASK 2: Advanced FastViT Architecture Integration

**State Acknowledgment**: You must begin your response by explicitly stating: "Executing Task 2: Advanced FastViT Architecture Integration."

**Execution Directive**:
1. Construct an isolated micro-experiment script located at `scripts/verify_fastvit.py`.
2. Within this script, utilize the `timm` (PyTorch Image Models) library to instantiate the `fastvit_t8` lightweight architecture. Generate a randomized dummy tensor with the exact shape profile `` and process it through the network. Utilize `structlog` to output the exact dimensional shape of the resulting output embeddings to verify library compatibility.
3. Access `src/models/distillation.py`. Implement the advanced `FastViTStudent` class architecture.
4. The class constructor must initialize the FastViT backbone and immediately append a custom multi-layer projection head. This head must mathematically project the native FastViT feature dimensionalities into the exact semantic embedding space defined by the Qwen2.5-VL teacher network.
5. You must implement a utility method within the class named `apply_structural_reparameterization`. This method must mathematically fuse the multi-branch convolution operations and the batch normalization parameters into single execution nodes, preparing the model for extreme edge compilation via the MIGraphX engine.

**Static Analysis Gate:**

Execute the command `ruff check --fix`. followed by pyright.. You must resolve any syntax or typing errors before proceeding. Output: "Static Analysis: PASSED".

**Validation Gate:**

Halt generation completely. Request that the human operator execute `python scripts/verify_fastvit.py` and provide the terminal telemetry output. Await explicit authorization before proceeding.

---

## TASK 3: Autonomous Static Visual Tracking Generation

**State Acknowledgment**: You must begin your response by explicitly stating: "Executing Task 3: Autonomous Static Visual Tracking Generation."

**Execution Directive**:
1. Create a highly specialized utility script located at `src/utils/visualizer.py`.
2. Implement the `StaticModelCardGenerator` class. This class must possess a method capable of ingesting the aggregated LOOCV metrics (accuracy, AUROC, precision, recall), the complete array of decision scores across all thirty-four iterations, and the corresponding ground truth arrays.
3. Utilize the `matplotlib` library to algorithmically render two specific high-density visualizations:
   * A highly detailed one-dimensional strip plot separating the decision scores by class, providing immediate visual verification of the SVM separating hyperplane margin.
   * A clear, heavily formatted bar chart summarizing the numerical classification metrics.
4. The generation method must absolutely avoid saving raw image files to the local disk. Instead, the method must save the rendered figures directly into memory utilizing the `io.BytesIO` buffer, encode the byte payload into standardized Base64 formatting strings, and embed the strings directly into standard HTML `<img>` tags within a dynamically generated Python string template.
5. Ensure the script writes the final compiled string to a localized file named `pipeline_metrics_report.md` within the repository root. Ensure the code rigorously executes `plt.close()` calls to neutralize potential memory leaks.
6. Access `src/models/classifier.py` and inject an execution call to the `StaticModelCardGenerator` directly following the conclusion of the cross-validation optimization loop.

**Static Analysis Gate:**

Execute the command `ruff check --fix` followed by `pyright`. You must resolve any syntax or typing errors before proceeding. Output: "Static Analysis: PASSED".

**Validation Gate:**

Halt generation completely. Ask the human operator for authorization to transition to Task 4.

---

## TASK 4: Hierarchical State Machine Serialization

**State Acknowledgment**: You must begin your response by explicitly stating: "Executing Task 4: Hierarchical State Machine Serialization."

**Execution Directive:**

1. Generate a new markdown document within the project root directory titled `PIPELINE_STATE.md`.
2. You are tasked with populating this document with a flawless PlantUML hierarchical state machine representation detailing the entire defect detection architecture.
3. You must enclose the diagram exclusively within a markdown fenced code block, utilizing three backticks followed strictly by the `plantuml` language identifier.
4. Implement composite structures for the dual pipeline using the exact syntax `state Phase2_DualPipeline {... }`. You must map the parallel execution branches separating the DINOv2 classification pipeline from the FastViT semantic distillation pipeline.
5. You must enforce the inclusion of the `hide empty description` modifier at the very beginning of the PlantUML definition block to guarantee clean execution within the JetBrains rendering engine.

**Static Analysis Gate**:

Execute the command `ruff check --fix` followed by `pyright`. You must resolve any syntax or typing errors before proceeding. Output: "Static Analysis: PASSED".

**Validation Gate**:

Halt generation completely. Formally inform the human operator that the comprehensive modification protocol has successfully concluded and await final sign-off.
