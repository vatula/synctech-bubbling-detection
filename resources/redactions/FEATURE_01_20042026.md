Role: You are an Expert MLOps and Data Science Python Developer.
Task: Write a Python script named `generate_final_report.py` that automatically compiles a comprehensive Markdown report (`Final_SyncTech_Evaluation_Report.md`) demonstrating the complete lifecycle, metrics, and qualitative results of our anomaly detection pipeline.

### Audit Ledger (Implementation Tasks)
- [x] **Task 0.1:** Validate file path existence for all required artifacts (`pipeline_metrics_report.md`, `phase5_consolidated_report.md`, `phase5_distillation_loss_curve.png`, and image directories).
- [x] **Task 0.2:** Implement `MarkdownReportGenerator` class to handle sections 1-4, ensuring robust image path validation and Markdown formatting.
- [x] **Task 0.3:** Implement data extraction utilities for parsing telemetry metrics from `pipeline_metrics_report.md` and `phase5_consolidated_report.md`.
- [x] **Task 0.4:** Integrate required qualitative narratives: augmentation strategy, threshold calibration explanation, and "Topological Confusion" observations.
- [x] **Task 0.5:** Run `generate_final_report.py`, verify `Final_SyncTech_Evaluation_Report.md` content and structure, and finalize.

Context & Artifacts Available:
1. Metrics Reports: `pipeline_metrics_report.md` and `phase5_consolidated_report.md` are in the root directory.
2. Loss Curve: `phase5_distillation_loss_curve.png` is in the root directory.
3. Image Artifacts: Located in `results/Dinomaly/`. 
   - `bubbling/train-bubbling/` (contains original training images)
   - `bubbling/hard-negatives-bubbling/` (contains OOD/hard negative images)
   - `latest/` (contains triptychs: Original Image | Image + Anomaly Map | Image + Pred Mask)

Instructions for the Script Generation:

The script must create a Markdown file with the following exact sections and content logic:

## 1. Generated Training Records
- Instruct the script to locate one image from `results/Dinomaly/bubbling/train-bubbling/`.
- Add a text section explaining our data augmentation strategy: Explicitly state that strict D4 dihedral geometric transformations (90, 180, 270-degree rigid rotations and flips) and conservative brightness jitter were used. Explain that this acts as virtual dataset expansion, preventing the small dataset (34-37 images) from memorizing the training set while preserving specular highlights.

## 2. Before / After Results (Train vs. Evaluation)
- Embed the `phase5_distillation_loss_curve.png` image. Add a caption explaining that the semantic distillation loss curve demonstrates the Student VLM's contrastive loss over 100 epochs, showing a steep descent (Train) and a stable plateau (Evaluation), proving the model settled rather than overfitted.
- Instruct the script to locate a triptych image from `results/Dinomaly/latest/` that corresponds to a normal image. Add a narrative explaining the threshold calibration: compare the default threshold (which generates noisy bounding boxes) against our calculated `F1AdaptiveThreshold` (which cleans up false positives).

## 3. Metrics and OOD Tests
- Read and extract the final pipeline telemetry metrics from the provided `pipeline_metrics_report.md` and `phase5_consolidated_report.md` files. Format this extracted data into a clean Markdown table showing Model Architecture, Key Metric (AUROC/Precision/Recall), and Latency for Classification, Localization, and Semantic Check.
- **Out-of-Distribution (OOD) Tests & Qualitative Observations:** Instruct the script to locate a triptych corresponding to the image in `results/Dinomaly/bubbling/hard-negatives-bubbling/`. 
- **CRITICAL - Add this specific qualitative observation:** Write a paragraph about "Topological Confusion". Explicitly point out that the anomaly maps show "hot regions" on irrelevant background areas, such as the curtains. Explain that to an unsupervised Vision Transformer, dark folds in a curtain look topologically identical to dark micro-bubbles. Conclude that while localization precision is bottlenecked by this environmental noise, the subsequent semantic checks and SVM classification successfully filter these out, proving the robustness of the multi-stage pipeline.

## 4. The Continuous Iteration Loop (Active Learning)
- Add a final text section explaining the MLOps lifecycle once deployed to the edge.
- Define "The Uncertainty Zone" (e.g., 40% - 60% confidence). Explain that predictions falling in this range are flagged, cropped, and sent to a Human Inspector via a dashboard. Human feedback (Confirm/Reject) converts low-confidence predictions into ground-truth labels for the next training iteration.
- Define "The Stop Criteria": 1) Metric Plateau (performance on a holdout set hits target and stops improving) and 2) Uncertainty Depletion (volume of images in the Uncertainty Zone drops below an operational threshold, proving the model has generalized).

Ensure the Python script uses standard libraries (like `os`, `re`, `shutil`) to verify the existence of the image paths and correctly format the Markdown image links based on relative paths.