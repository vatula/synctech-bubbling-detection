# Implementation Instructions for `EDITS_19042026.md`

## Phase 1 — Dataset Robustness and Structural Encapsulation (Task 1)

- [x] Create a temporary micro-experiment notebook `notebooks/task1_loader_contract_check.py` that instantiates `BubblingDataset` with:
  - a valid Albumentations pipeline returning `torch.Tensor`
  - an intentionally invalid pipeline returning non-tensor output
  Deliverable: notebook run log proving one success path and one strict failure path (`RuntimeError` raised with structured payload).

- [x] Refactor `src/data/loader.py` `BubblingDataset.__getitem__` to remove permissive fallback conversion logic and enforce strict tensor contract.
  Deliverable: `__getitem__` raises `RuntimeError` when transformed output is not `torch.Tensor`, including structured keys (`event`, `index`, `image_path`, `received_type`).

- [x] Add module-level telemetry in `src/data/loader.py`:
  - `import structlog`
  - `logger = structlog.get_logger()`
  Deliverable: module-level logger defined and used for dataset initialization telemetry.

- [x] Add class-initialization telemetry in `BubblingDataset` reporting class counts.
  Deliverable: structured log event with exact counts for nominal and bubbling samples.

- [x] Populate `src/data/__init__.py` and `src/models/__init__.py` with explicit exports only.
  Deliverable: both files include typed import/export surface and `__all__` entries for public classes (`BubblingDataset`, `BubblingClassifier`, `FastViTStudent`, etc. as actually implemented).

- [x] Eliminate magic numbers introduced/used in this phase by moving them into named constants with explanatory comment blocks.
  Deliverable: all constants used in modified files are named and documented (`why value exists`, `expected range`, `impact if changed`).

### Phase 1 Micro-tests

- [x] Contract test: valid transform returns tensor and sample is consumable by downstream model input pipeline.
  Deliverable: passing test/assertion output.

- [x] Contract test: invalid transform output type triggers deterministic `RuntimeError`.
  Deliverable: captured exception payload with expected fields.

- [x] Telemetry test: dataset initialization emits single structured log with class distribution.
  Deliverable: log capture containing expected keys and integer values.

- [x] Determinism test: repeated indexing returns stable type/shape for same sample under fixed random seed.
  Deliverable: repeated assertions for dtype, shape, and label consistency.

- [x] Performance parity check (local vs container): compare dataloader startup time and batch fetch latency for same batch size and resolution.
  Deliverable: markdown table with local/container metrics and delta threshold decision.

### Phase 1 Quality Gates

- [x] Run `ruff check --fix .` and `ruff format .`.
  Deliverable: zero lint violations.

- [x] Run `pyright`.
  Deliverable: zero typing errors.

- [x] Delete temporary micro-experiment file(s) from `notebooks/` after results are recorded.
  Deliverable: no temporary phase notebook remains in `notebooks/`.

---

## Phase 2 — Advanced FastViT Architecture Integration (Task 2)

- [x] Create temporary micro-experiment script `notebooks/verify_fastvit.py` to instantiate `fastvit_t8` via `timm`, run one randomized tensor through the model, and log output embedding shape with `structlog`.
  Deliverable: execution log containing input shape, output shape, dtype, and device.

- [x] Replace ambiguous shape assumptions with named constants (input height/width/channels, teacher embedding dimension) and explanatory comment blocks.
  Deliverable: constants module/section with rationale and source provenance for each value.

- [x] Implement `FastViTStudent` in `src/models/distillation.py`:
  - initialize FastViT backbone
  - append projection head mapping backbone features to teacher semantic embedding space
  Deliverable: class with fully typed constructor/forward and documented projection dimensions.

- [x] Implement `apply_structural_reparameterization` in `FastViTStudent` to fuse supported conv+BN branches into inference-ready structure.
  Deliverable: method with explicit preconditions, idempotent behavior, and return contract.

- [x] Ensure SOLID compliance:
  - single-responsibility separation between feature extraction, projection, and reparameterization
  - dependency injection points for backbone name and embedding size
  Deliverable: class API signatures showing separated responsibilities.

### Phase 2 Micro-tests

- [x] Shape compatibility test: forward pass output dimension exactly matches teacher embedding dimension.
  Deliverable: assertion output with expected/actual dimensions.

- [x] Reparameterization invariance test: pre/post reparameterization output cosine similarity above configured threshold on fixed inputs.
  Deliverable: metric report with threshold pass/fail.

- [x] Training stability smoke test: one optimizer step without NaN/Inf in loss or gradients.
  Deliverable: logged finite loss and gradient checks.

- [x] Throughput/latency check (local vs container): compare forward latency on identical tensor shape and dtype.
  Deliverable: local/container latency table and consistency verdict.

- [x] Memory footprint check: capture peak memory during micro-batch inference.
  Deliverable: peak memory values and regression comparison against baseline.

### Phase 2 Quality Gates

- [x] Run `ruff check --fix .` and `ruff format .`.
  Deliverable: zero lint violations.

- [x] Run `pyright`.
  Deliverable: zero typing errors.

- [x] Delete temporary micro-experiment file(s) from `notebooks/` after validation.
  Deliverable: no temporary phase notebook/script remains in `notebooks/`.

---

## Phase 3 — Autonomous Static Visual Tracking Generation (Task 3)

- [x] Implement `src/utils/visualizer.py` with `StaticModelCardGenerator` and typed API for metrics, decision scores, and ground truth arrays.
  Deliverable: class implementation with explicit input schema validation.

- [x] Implement in-memory figure rendering pipeline using `io.BytesIO` + Base64 encoding and HTML `<img>` embedding (no raw image file persistence).
  Deliverable: markdown-compatible string containing embedded Base64 image tags.

- [x] Build both required visualizations:
  - class-separated 1D decision-score strip plot
  - formatted classification metric bar chart
  Deliverable: generated plots embedded into report payload.

- [x] Write final report to repository root as `pipeline_metrics_report.md` and enforce `plt.close()` after each figure.
  Deliverable: report file exists and memory-safe plotting calls verified.

- [x] Integrate generator call into `src/models/classifier.py` immediately after LOOCV completion.
  Deliverable: classifier execution path triggers report generation with complete metric context.

- [x] Minimize complexity and preserve model quality trajectory by keeping visualization side effects isolated from training/evaluation logic.
  Deliverable: no metric computation logic moved into visualization layer; only read-only consumption.

### Phase 3 Micro-tests

- [x] Input validation test: reject malformed metric payloads and score/label length mismatches.
  Deliverable: deterministic exceptions with descriptive error messages.

- [x] Report generation test: markdown output includes both Base64 image tags and scalar metrics.
  Deliverable: assertions on report content structure.

- [x] Memory hygiene test: repeated generation loop does not leak matplotlib figure handles.
  Deliverable: figure count remains bounded after repeated runs.

- [x] Integration test: LOOCV completion triggers exactly one report generation call.
  Deliverable: call-count assertion/log evidence.

- [x] Performance parity check (local vs container): compare report generation runtime and output byte size for same inputs.
  Deliverable: runtime/size comparison table and acceptable variance decision.

### Phase 3 Quality Gates

- [x] Run `ruff check --fix .` and `ruff format .`.
  Deliverable: zero lint violations.

- [x] Run `pyright`.
  Deliverable: zero typing errors.

- [x] Delete temporary micro-experiment file(s) from `notebooks/` after validation.
  Deliverable: no temporary phase notebook/script remains in `notebooks/`.

---

## Phase 4 — Hierarchical State Machine Serialization (Task 4)

- [x] Create `PIPELINE_STATE.md` in repository root.
  Deliverable: markdown file committed with PlantUML diagram only.

- [x] Author a PlantUML hierarchical state machine enclosed in a fenced block using `plantuml` language identifier and include `hide empty description` at the top.
  Deliverable: syntactically valid PlantUML section ready for JetBrains rendering.

- [x] Model dual-pipeline composition using exact composite notation `state Phase2_DualPipeline { ... }` with parallel branches for DINOv2 classification and FastViT semantic distillation.
  Deliverable: explicit branch states/transitions for both paths and synchronization into unified output state.

- [x] Encode constants (phase names, transition labels, terminal states) as documented labels to avoid magic naming and improve repeatability.
  Deliverable: consistent naming glossary section or inline comments in markdown.

- [x] Add repeatability check instructions so the diagram can be regenerated/reviewed in local and container environments with equivalent rendering behavior.
  Deliverable: checklist for local/container rendering validation.

### Phase 4 Micro-tests

- [x] Syntax test: PlantUML block parses without errors in JetBrains preview.
  Deliverable: successful render confirmation.

- [x] Structural test: `Phase2_DualPipeline` contains both required branch paths and reconverges to unified response state.
  Deliverable: checklist confirmation against architecture requirements.

- [x] Runtime parity check (local vs container): render success and diagram hash/text equivalence across environments.
  Deliverable: parity result with pass/fail status.

- [x] Regression test: transitions cover all phase handoffs from ingestion to final payload.
  Deliverable: transition coverage checklist.

### Phase 4 Quality Gates

- [x] Run `ruff check --fix .` and `ruff format .`.
  Deliverable: zero lint violations (repository-wide gate remains clean).

- [x] Run `pyright`.
  Deliverable: zero typing errors.

- [x] Delete temporary micro-experiment file(s) from `notebooks/` after validation.
  Deliverable: no temporary phase notebook/script remains in `notebooks/`.

---

## Global Acceptance Checklist

- [ ] Each phase preserves or improves measurable model quality metrics; no degradation is accepted without documented rollback or mitigation.
  Deliverable: before/after metrics comparison per phase.

- [x] All telemetry uses `structlog`; no `print()` usage exists in modified scope.
  Deliverable: search evidence and/or lint rule pass.

- [ ] Local and container performance tracking artifacts are captured for every phase where runtime is involved.
  Deliverable: consolidated parity matrix with thresholds and variance outcomes.

- [ ] All temporary micro-experiment artifacts are removed after each phase.
  Deliverable: clean `notebooks/` directory with only permanent notebooks retained.